# Lab Manifests

Lab manifests describe the relationship between machines that belong to one Crucible lab.

Current releases support one VM per Forge invocation, so this layer remains minimal.

Future multi-VM releases are expected to use lab manifests for shared networks, machine relationships, configuration assignments, startup ordering, and saved topology state.
