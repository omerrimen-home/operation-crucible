# Crucible Provisioning

This package contains Crucible's unattended operating-system provisioning backends.

Current implementations include Ubuntu Subiquity/NoCloud autoinstall, Kali Debian Installer preseeding, Windows Unattend generation, temporary Kali preseed delivery, and installation-media detection.

Provisioning converts a resolved machine manifest into the files and media required for zero-touch guest installation.