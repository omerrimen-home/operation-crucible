# Crucible Inventory

This package is reserved for future internal inventory and topology-management functionality.

Current runtime Ansible inventory generation remains part of the Forge workflow and writes its output below `.crucible/`.

Future multi-VM development may move additional machine and topology inventory logic into this package.
