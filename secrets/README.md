# Secrets

Do not commit real credentials, private keys, passwords, tokens, or other sensitive material to this directory.

Current machine-specific secrets and SSH identities are stored under `.crucible/`, which is excluded from Git.

This directory is reserved for future secret-management integration or non-sensitive documentation/examples.
