
# Ansible Playbooks

This directory contains Crucible playbooks used for post-install configuration.

Base VM creation, unattended installation, bootstrap, and management connectivity are handled by the Forge before these playbooks run.

Beginning with the v0.3 configuration system, playbooks will orchestrate reusable Crucible roles after Ansible connectivity has been verified.
