# Ansible Roles

This directory is reserved for reusable post-install Crucible configuration roles.

Roles should be idempotent, operating-system aware, and driven by machine or configuration variables.

Planned roles include system hardening, nftables, Active Directory, DNS, DHCP, and other reusable cybersecurity-lab services.
